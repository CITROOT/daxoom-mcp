# Assets

Add `logo.png` here — a **400×400 PNG** of the Daxoom logo. LobeHub (and some
other marketplaces) require a square logo for the listing; the README and
`mcp-server.json` can reference `assets/logo.png` once added.
